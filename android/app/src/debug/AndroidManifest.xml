<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter tooling needs a network socket to talk to the running app in
         debug/profile builds only. The release build has no INTERNET permission. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
